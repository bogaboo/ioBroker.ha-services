{
  "name": "iobroker.ha-services",
  "version": "0.0.1",
  "description": "Home Assistant Service API Adapter for ioBroker",
  "author": "Dein Name",
  "license": "MIT",
  "type": "commonjs",
  "main": "build/main.js",
  "files": [
    "build/",
    "admin/",
    "io-package.json",
    "LICENSE"
  ],
  "scripts": {
    "clean": "rimraf build",
    "build": "npm run clean && tsc -p tsconfig.json",
    "watch": "tsc -w",
    "lint": "eslint src --ext .ts",
    "test": "mocha",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@iobroker/adapter-core": "^3.2.3",
    "axios": "^1.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "eslint": "^9.0.0",
    "mocha": "^11.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
